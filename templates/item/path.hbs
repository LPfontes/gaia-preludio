<section class="gaia-sheet-content">
  <div class="inventory-table-panel tab-path-content">
    <div class="inventory-table-header">
      <div class="item-header-group">
        <img class="item-mini-icon" src="{{item.img}}" data-action="editImage" data-edit="img" title="{{item.name}}"/>
        <h4 class="inventory-table-title">
          <input name="name" type="text" value="{{item.name}}" placeholder="{{localize 'GAIA.Path.NamePlaceholder'}}"/>
        </h4>
      </div>
    </div>

    <div class="path-table-frame">
      <table class="inventory-grid-table">
        <thead>
          <tr>
            <th style="width: 30%;">{{localize "GAIA.Path.Keywords"}}</th>
            <th style="width: 33%;">{{localize "GAIA.Path.ParameterSuggestions"}}</th>
            <th style="width: 12%;">{{localize "GAIA.Path.AbilitiesCount"}}</th>
            <th class="col-edit" style="width: 36px; text-align: center;"><i class="fa-solid fa-gear"></i></th>
          </tr>
        </thead>
        <tbody>
          <tr class="item-row">
            <td>
              <input type="text" name="system.keywords" value="{{system.keywords}}" placeholder="{{localize 'GAIA.Path.KeywordsPlaceholder'}}" />
            </td>
            <td>
              <input type="text" name="system.parameterSuggestions" value="{{system.parameterSuggestions}}" placeholder="{{localize 'GAIA.Path.ParameterSuggestionsPlaceholder'}}" />
            </td>
            <td style="text-align: center; font-weight: bold; color: var(--gaia-text-parchment);">
              {{pathAbilities.length}}
            </td>
            <td class="col-edit" style="text-align: center; vertical-align: middle;">
              <button type="button" class="btn-toggle-table-edit" data-action="toggleTableEdit" title="Editar" style="cursor: pointer; background: transparent; border: none; color: var(--gaia-text-parchment);"><i class="fa-solid fa-pen"></i></button>
            </td>
          </tr>
        </tbody>
      </table>
    </div>

    <div class="path-specializations-panel">
      <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
        <label class="description-title">{{localize "GAIA.Path.Specializations"}}</label>
        <button type="button" class="btn-toggle-description-edit" data-action="toggleDescriptionEdit" title="Editar" style="cursor: pointer; background: transparent; border: none; color: var(--gaia-text-parchment); padding: 0 4px;"><i class="fa-solid fa-pen"></i></button>
      </div>
      <textarea name="system.specializations" class="path-specializations-textarea" placeholder="{{localize 'GAIA.Path.SpecializationsPlaceholder'}}">{{#if formattedSpecializations}}{{formattedSpecializations}}{{else}}{{system.specializations}}{{/if}}</textarea>
    </div>

    <div class="path-description-panel">
      <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
            <label class="description-title">{{localize "GAIA.Path.Description"}}</label>
            <button type="button" class="btn-toggle-description-edit" data-action="toggleDescriptionEdit" title="Editar" style="cursor: pointer; background: transparent; border: none; color: var(--gaia-text-parchment); padding: 0 4px;"><i class="fa-solid fa-pen"></i></button>
          </div>
          <textarea name="system.description" class="path-description-textarea" placeholder="{{localize 'GAIA.Path.DescriptionPlaceholder'}}">{{system.description}}</textarea>
    </div>

    <div class="path-abilities-section">
      <div class="section-header">
        <h3 class="panel-main-title">{{localize "GAIA.Path.AbilitiesTitle"}}</h3>
        <button type="button" class="btn-add-ability" data-action="addPathAbility" title="{{localize 'GAIA.Path.AddAbilityTitle'}}">
          <i class="fa-solid fa-plus"></i> {{localize "GAIA.Path.AddAbilityBtn"}}
        </button>
      </div>

      {{#if pathAbilities.length}}
        {{#each pathAbilityCategories as |group|}}
          <div class="path-category-group">
            <div class="path-category-header">
              <span class="path-category-title">{{group.label}}</span>
              <span class="path-category-count">{{group.abilities.length}}</span>
            </div>
            <div class="path-category-grid">
              {{#each group.abilities as |ability|}}
                <div class="path-ability-card" draggable="true" data-action="editPathAbility" data-index="{{ability.index}}" data-entry-id="{{ability.id}}" data-uuid="{{ability.uuid}}" title="Arraste para a ficha de personagem ou clique para abrir">
                  <div class="path-ability-header">
                    <div class="path-ability-identity">
                      <img class="path-ability-icon" src="{{#if ability.img}}{{ability.img}}{{else}}icons/svg/item-bag.svg{{/if}}" draggable="false" title="{{ability.name}}"/>
                      <div class="path-ability-titles">
                        <h4 class="path-ability-title">{{ability.name}}</h4>
                        <div class="path-ability-meta-badges">
                          {{#if ability.cost}}<span class="badge cost-badge">{{ability.cost}}</span>{{/if}}
                          {{#if ability.actionTypeLabel}}<span class="badge action-badge">{{ability.actionTypeLabel}}</span>{{/if}}
                          {{#if ability.typeLabel}}<span class="badge type-badge">{{ability.typeLabel}}</span>{{/if}}
                        </div>
                      </div>
                    </div>
                    <div class="path-ability-controls">
                      <button type="button" class="btn-remove-ability" data-action="removePathAbility" data-index="{{ability.index}}" draggable="false" title="{{localize 'GAIA.Path.RemoveAbilityTitle'}}">
                        <i class="fa-solid fa-trash"></i>
                      </button>
                    </div>
                  </div>
                </div>
              {{/each}}
            </div>
          </div>
        {{/each}}
      {{else}}
        <p class="empty-hint">{{localize "GAIA.Path.EmptyAbilitiesHint"}} (Ou arraste um Item do tipo Habilidade para cá)</p>
      {{/if}}
    </div>
  </div>
</section>

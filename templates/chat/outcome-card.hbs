<div class="gaia-outcome-card {{outcomeClass}}">
  <div class="outcome-header">
    <strong>{{actor.name}}</strong>
  </div>
  <div class="outcome-title">{{outcomeTitle}}</div>
  <div class="formula-display">{{formula}} ➜ {{total}} (Dif. {{dc}})</div>
</div>
